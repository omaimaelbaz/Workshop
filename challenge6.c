#include <stdio.h>
int main ( ) {
    float r ;
    float circonference ;
    printf("ernter le rayon :");
    scanf("%f" , &r);
    printf (" trouver la circonference d'un cercle !! \n");
    circonference = 2 *3.14 *r ;
    printf (" la circonference du cercle est : %f ",circonference);



}