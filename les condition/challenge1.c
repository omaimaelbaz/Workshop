#include <stdio.h>
int main()
{
    int num ;
    printf("enter un nomber :" );
    scanf("%d" ,&num);
    if ( num % 2 == 0)
        {
            printf("ce nombre est paire ");
        }
        else 
          printf("ce nombre est impaire ");
            

}